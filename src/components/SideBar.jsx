import React from "react";
import { Container } from "../assets/style/homeElements";

const SideBar = ({ isOpen, toggle }) => {
  return (
    <>
      <Container $mode="side"></Container>
    </>
  );
};

export default SideBar;
